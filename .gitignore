# PDFs — too large for git; copy pdfs/ folder separately or use git-lfs
pdfs/*.pdf

# macOS
.DS_Store
**/.DS_Store

# Python
__pycache__/
*.pyc
*.pyo

# Editor
.vscode/
.idea/

# Offline map data — large, distributed on the USB/extracted locally (like PDFs)
maps/*.pmtiles
/pmtiles

# Private hosting config (never commit personal infra)
.env.deploy

# Build output (generated ZIP packages)
downloads/
*.zip
dist/
